<Page
    NavigationCacheMode="Required"
    x:Class="Assistools.Pages.PageStartup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Padding="40,28,40,0" Spacing="4">
            <TextBlock Text="Démarrage &amp; services" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold" />
            <TextBlock
                Text="Désactivez les programmes et services qui se lancent automatiquement avec Windows."
                Style="{ThemeResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- ONGLETS -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="4" Padding="40,12,40,0">
            <Button x:Name="BtnDemarrage" Content="Programmes au démarrage"
                    Style="{ThemeResource AccentButtonStyle}" Click="BtnDemarrage_Click" />
            <Button x:Name="BtnServices" Content="Services tiers"
                    Click="BtnServices_Click" />
        </StackPanel>

        <!-- CONTENU -->
        <ScrollViewer Grid.Row="2" Padding="40,12,40,24">
            <StackPanel x:Name="ContentPanel" Spacing="4" />
        </ScrollViewer>

        <!-- Overlay chargement -->
        <Border Grid.Row="2" x:Name="LoadingOverlay" Visibility="Collapsed"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Opacity="0.8">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <ProgressRing IsActive="True" Width="36" Height="36" />
                <TextBlock x:Name="LoadingText" Text="Chargement…" HorizontalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
